# Что нужно от тебя — чеклист

Это всё, чего я жду от тебя (владельца), чтобы двигать Centry дальше. Разбито по приоритету:

- 🔴 **Блокирует следующий шаг** (создание код-скелета, этап 1: каркас + SQLite + денежный модуль).
- 🟡 **Нужно скоро** (в ходе Build 0).
- 🟢 **Нужно к v1.0** (App Store), сейчас можно игнорировать.

Где я предлагаю дефолт — можно просто ответить «ок», и я возьму его.

---

## 0. Первым делом

1. 🔴 **Ревью вольта.** Пробеги `CLAUDE.md` и `docs/` — совпадает ли с твоим видением? Особенно [DATA_MODEL](docs/DATA_MODEL.md), [DESIGN_SYSTEM](docs/DESIGN_SYSTEM.md), [ARCHITECTURE](docs/ARCHITECTURE.md). Дай «ок» или правки — после этого создаю код-скелет.

---

## 1. Продуктовые значения (🔴 — нужны для этапа 1)

Дефолты взяты из брифа; подтверди или поправь одним словом.

2. 🔴 **Минимальная цель iOS.** Предлагаю **iOS 15.1** (как Bsuir) с graceful fallback, Liquid Glass — только iOS 26+. Альтернатива — поднять флор до iOS 17/18, чтобы меньше возни с фолбэками. **Важно:** твой телефон/симулятор на какой iOS?
3. 🔴 **Список валют для Build 0.** Дефолт: **BYN, USD, EUR**. Нужны ещё (RUB, PLN, …)?
4. 🔴 **Стартовый курс USD→BYN.** В брифе пример `rate_usd_byn_e6 = 3270000` (т.е. 3.27). Дай актуальное значение на сегодня (и курсы для остальных валют из п.3, если они не BYN/USD).
5. 🔴 **Базовая валюта** — BYN. Подтверждаешь?
6. 🔴 **День зарплаты** — дефолт **1 число**. Ок? (Спрашивается в приложении при первом тапе по главной цифре, но нужен дефолт.)
7. 🔴 **Пороги для главной цифры** «можно сегодня» (зелёный → жёлтый → красный). Как красить? Предлагаю: зелёный, если остаток на день ≥ дневного норматива; жёлтый — если 50–100%; красный — если < 50% или ушёл в минус. Дай свою логику/проценты.
8. 🟡 **Переводы между счетами** (`kind = transfer`) в Build 0. В шите ввода есть тумблер «перевод». Подтверди, что перевод нужен уже в Build 0, и как считать курс при переводе между разными валютами (обе ноги по своим курсам на момент? комиссий нет?).
9. 🟡 **Виды счетов** — `card | cash | wallet`. Достаточно? Иконки для каждого вида — выберу из SF Symbols/эмодзи, если не против.
10. 🟡 **Вечернее напоминание** — дефолт **22:00**. Ок?

## 2. GitHub и репозиторий

11. 🔴 **Создать пустой репозиторий** `vazonhub/centry-wallet` на GitHub (или скажи, что создашь сам / уже создан) и дай мне знать remote URL. Сейчас локально есть только `.git` без remote, текущая ветка `master`.
12. 🟡 **Git flow.** Завести ветки `develop` (дефолтная) / `testing` / `master`? Скажи — заведу и настрою branch protection (скрипт по образцу Bsuir). Либо на Build 0 работаем прямо в одной ветке — тоже вариант.
13. 🟡 **Трекинг задач** — как в Bsuir через GitHub Issues, или достаточно `docs/BUILD0_PLAN.md`?

## 2а. Нативка (этапы 7–8) — что нужно, чтобы собрать на устройстве

Приложение запускается на симуляторе, вечерний пуш работает. Виджет (этап 7) ждёт сборки на устройстве. Siri (этап 8) временно отключён — см. ниже. Чтобы собрать:

- 🔴 **Apple Team ID.** Нужен для подписи виджет-таргета (`@bacons/apple-targets`). Плоскость уже проведена: значение читается из `.env` (`APPLE_TEAM_ID=`) через `app.config.ts` — как `EXPO_OWNER`/`EAS_PROJECT_ID` (owner-specific, не коммитим в `app.json`). **Тебе нужно:** найти Team ID (10 символов, напр. `A1B2C3D4E5`) в Xcode (Settings → Accounts → твоя команда) или на developer.apple.com → Membership и вписать в `.env`.
- 🟡 **Сборка:** заполнить `APPLE_TEAM_ID` в `.env` → `npm run prebuild` (регенерит `ios/` + виджет-таргет + `pod install`) → открыть `ios/Centry.xcworkspace` в Xcode → своё устройство → Run.
  - **Виджет** добавляется на домашний экран стандартно (long-press → «+»).
  - **Вечерний пуш:** при первом запуске приложение запросит разрешение на уведомления; напоминание придёт ежедневно во время из «Настройки → Ввод → Время напоминания» (по умолчанию 22:00), тап открывает шит ввода. Всё локально — сеть не трогается.
- ⚠️ **Платный Apple Developer** почти наверняка нужен: App Groups + WidgetKit на бесплатном personal team обычно недоступны (см. п. 17).
- ✅ **Сборочные блокеры виджета — решены (2026-08-21):** `import MMKVAppExtension` + `pod 'MMKVAppExtension', :modular_headers => true` (под — статичный Obj-C++ без modulemap; без modular headers Swift не видит модуль). Ошибки `no such module 'MMKV' / 'MMKVAppExtension'` устранены.
- ⚠️ **Siri / App Intents — отключён (2026-08-21).** Первая реализация линковала `MMKVAppExtension` второй раз (в main-приложение — виджет уже линкует его), из-за чего в одном процессе оказывалось два потребителя `MMKVCore` → **порча кучи и краш на старте**. Плагин убран, приложение снова запускается. JS-часть готова и бездействует; вернёмся к Siri отдельно с безопасным каналом Swift→JS. Вечернего напоминания для «ядра ввода» на первое время достаточно.

## 3. Локальная разработка (🔴 — чтобы запускать сборку)

14. 🔴 **Mac + Xcode.** Подтверди, что есть Mac с Xcode (желательно 26.x для iOS 26 Liquid Glass) и версия.
15. 🔴 **Физический iPhone** для Build 0 (ставим через Xcode) — модель и версия iOS.
16. 🟡 **Node 22** и npm — стоят? (CI на Node 22.)
17. 🟡 **Apple ID для подписи в Xcode.** ⚠️ Важный нюанс: **App Groups + виджет (WidgetKit) + App Intents требуют, скорее всего, платного Apple Developer Program ($99/год)** даже для установки на свой телефон — бесплатный personal team часто не даёт App Groups и app extensions. Если платного аккаунта нет — виджет/Siri в Build 0 придётся отложить, а основное приложение поставим и без них. Скажи, есть ли платный аккаунт.

## 4. Ассеты и брендинг

18. 🟡 **Иконка приложения.** Есть готовая, или сделать плейсхолдер (буква C в Liquid-Glass стиле) на Build 0? Финальная — к v1.0.
19. 🟡 **Splash-экран** — плейсхолдер под токены дизайн-системы ок?
20. 🟢 **Шрифты.** Числа — системный SF Mono (tabular-nums), текст — SF Pro. Кастомных шрифтов не тянем (правило «ноль сети»). Подтверди, что системных достаточно.

## 5. К v1.0 (🟢 — сейчас не трогаем)

21. 🟢 **Apple Developer Program** оформлен (если ещё нет по п.17).
22. ✅ **Резервация имени — сделано (2026-08-20).** Имя = **`Centry`** (без дескриптора, B16). `ascAppId = 6803400593`, App Group `group.by.vazon.centry` привязан к `by.vazon.centry` и `by.vazon.centry.widget` (B17). `ascAppId` вписан в `eas.json`. → [`docs/GUIDE_APPSTORE_NAME.md`](docs/GUIDE_APPSTORE_NAME.md).
23. 🟢 **EAS / Expo:** аккаунт, `eas init` → `EAS_PROJECT_ID` + `EXPO_OWNER`, секрет `EXPO_TOKEN` в GitHub Actions. После этого включаю EAS/release-пайплайн. → пошагово: [`docs/GUIDE_EAS_SETUP.md`](docs/GUIDE_EAS_SETUP.md).
24. 🟢 **Приватность App Store** (nutrition label): т.к. данные не покидают телефон — «Data Not Collected». → пошагово + готовый текст политики: [`docs/GUIDE_PRIVACY.md`](docs/GUIDE_PRIVACY.md).

---

## Как отвечать

Достаточно пройтись по 🔴-пунктам (п. 2–7, 11, 14–15) — этого хватит, чтобы начать код-скелет и этап 1. Остальное можно закрывать по ходу. На всё, где написано «дефолт» — можно ответить просто «дефолты ок».
