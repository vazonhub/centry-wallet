// Expo Router is the app entry. Unlike Bsuir Time there is no Android widget
// task handler to register — Centry is iOS-only (B3) and its widget is a
// native WidgetKit target that reads a snapshot, not an RN widget.
import 'expo-router/entry';
